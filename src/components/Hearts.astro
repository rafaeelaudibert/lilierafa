---
import heartSvg from '../assets/heart.svg?raw';
---

<div
  class="text-gray-400 flex justify-center items-center gap-2"
  aria-hidden="true"
>
  <span class="text-2xl animate-pump" style="animation-delay: 0.1s;" set:html={heartSvg.replace('<svg', '<svg width="1em" height="1em"')}></span>
  <span class="text-4xl animate-pump" style="animation-delay: 0s;" set:html={heartSvg.replace('<svg', '<svg width="1em" height="1em"')}></span>
  <span class="text-2xl animate-pump" style="animation-delay: 0.2s;" set:html={heartSvg.replace('<svg', '<svg width="1em" height="1em"')}></span>
</div>

<style>
  @keyframes pump {
    0%,
    100% {
      transform: scale(1);
    }
    5% {
      transform: scale(1.2);
    }
    10% {
      transform: scale(1);
    }
  }
  .animate-pump {
    display: inline-block;
    animation: pump 3s infinite;
  }
</style>
